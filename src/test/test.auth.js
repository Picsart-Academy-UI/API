const { expect } = require('chai');
const { describe, it } = require('mocha');

describe('Array', () => {
  describe('#indexOf()', () => {
    it('should test', () => {
      expect(true).to.equal(true);
    });
  });
});
